server {
    listen 80 default_server;
    listen [::]:80 default_server;

    auth_basic "bw-serve";
    auth_basic_user_file /etc/nginx/htpasswd;

    location / {
        proxy_pass http://localhost:8087;
    }
}
